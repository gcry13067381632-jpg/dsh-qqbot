#!/bin/sh
# ⚠️ 此脚本已废弃（内容过时：包名/默认 profile 均不再匹配）。
# 请改用仓库根的 install.ps1（Windows 一键，面向 web profile）：
#   1. 发布到 npm registry 后：
#        dsh plugin --profile web add @zaofan/dsh-qqbot
#   2. 或源码分发（仓库根执行）：
#        .\install.ps1
echo "install.sh is deprecated. Use install.ps1 (Windows) or:"
echo "  dsh plugin --profile web add @zaofan/dsh-qqbot"
exit 1
